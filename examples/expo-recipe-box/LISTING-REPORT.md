# Recipe Box — listing-kit run report

Generated by running the **listing-kit** skill against this example app.
This is the Phase-B output described in [`../README.md`](../README.md).

## Run environment
- **Platforms:** iOS (App Store) **and** Android (Google Play).
- **Devices:**
  - iPhone 17 Pro Max simulator → **6.9"**, **1320×2868**.
  - **iPad Pro 13-inch simulator → 2064×2752** (captured because `ios.supportsTablet: true`).
  - Pixel 8 emulator → native **1080×2400**, cropped to **1080×1920** (finding 4).
- **Stack detected:** Expo / React Native (managed), Expo Router.
- **Build:** `expo run:ios` / `expo run:android` (debug dev-client + Metro). iPad reused the iOS build, installed on the iPad sim.
- **Drive:** deep links (`recipebox://…`); status bars sanitized to 9:41 / full battery & signal.
- **Locale:** en-US.

## Assets produced

| Asset | Count | Format | Status |
|---|---|---|---|
| iPhone 6.9" screenshots | 4 | 1320×2868, **RGB 24-bit no-alpha** | ✅ |
| iPad 13" screenshots | 4 | 2064×2752, RGB 24-bit no-alpha | ✅ |
| Android phone screenshots | 4 | 1080×1920, RGB 24-bit no-alpha | ✅ |
| Play feature graphic | 1 | 1024×500, RGB 24-bit no-alpha | ✅ |
| Play icon | 1 | 512×512, 32-bit (alpha — correct for icon) | ✅ |
| App Store + Play copy | — | within all char limits | ✅ |
| App icon artwork | — | Expo placeholder | ⚠️ replace before real submission |

All screenshots were **flattened from the raw 32-bit RGBA `simctl`/`adb` output to
24-bit RGB (no alpha, 8-bit depth)** to satisfy both stores. Secret-scan gate
(skill §9.1): **clean**.

### Character-limit validation
| Store | Field | Used / Limit |
|---|---|---|
| App Store | name / subtitle / promo / keywords / description | 23/30 · 28/30 · 76/170 · 79/100 · 673/4000 |
| Play | title / short / full | 23/30 · 69/80 · 673/4000 |

## Not yet generated (next actions)
- **Android tablet (7"/10") screenshots:** the app supports tablets, but the only
  Android AVD here is a **phone** (pixel_8). Capturing needs a tablet AVD
  (`pixel_tablet`) + system image. Play tablet shots are optional to publish.
- **Apple Watch / Wear OS:** app has no watch target — N/A.
- **App icon artwork:** still the Expo placeholder.
- **Localization:** en-US only.

## Dogfooding findings (fed back into the skill)
1. **Maestro device selection** — pass `--device <udid|serial>` on multi-device
   machines (Maestro defaulted to Android). → `references/driving/maestro.md`.
2. **`clearState` vs. Expo dev-client** — breaks debug builds (wipes Metro URL);
   capture from a **release** build. → `references/driving/maestro.md`.
3. **Deep-linking stacks navigation** — relaunch + one deep link per screen for clean
   back-stack labels. Cold-start deep links also raise an iOS "Open in app?" consent
   dialog (dismiss via Maestro / a running app).
4. **Play aspect-ratio limit** — 1080×2400 (2.22:1) exceeds Play's 2:1; cropped to
   1080×1920. → `references/stores/google-play.md`.
5. **Screenshot format** — raw `simctl`/`adb` output is **32-bit RGBA**, which both
   stores reject; must flatten to 24-bit RGB no-alpha. The feature-graphic script also
   emitted 16-bit depth — fixed to force `-depth 8`. → store docs + `SKILL.md` capture.
6. **iPad required when `supportsTablet`** — Apple requires iPad screenshots if the app
   runs on iPad; the first run missed this. → `references/stores/apple-app-store.md`.
